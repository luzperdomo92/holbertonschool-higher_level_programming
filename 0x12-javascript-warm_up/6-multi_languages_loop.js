#!/usr/bin/node
const phrases = ['C is fun', 'Python is cool', 'Javascript is amazing'];
const phrasesLen = phrases.length;
let iter;

for (iter = 0; iter < phrasesLen; iter++) {
  console.log(phrases[iter]);
}
